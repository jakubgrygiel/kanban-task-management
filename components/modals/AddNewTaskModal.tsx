import styled from "styled-components";
import ModalWrapper from "./ModalWrapper";

interface IAddNewTaskModalProps {}

export default function AddNewTaskModal() {
  return <ModalWrapper>Add new task</ModalWrapper>;
}
